import coinUrl from 'file!./coin.wav';
import crashUrl from 'file!./crash.wav';
import jumpUrl from 'file!./jump.wav';

export const coin = new Audio(coinUrl);
export const crash = new Audio(crashUrl);
export const jump = new Audio(jumpUrl);